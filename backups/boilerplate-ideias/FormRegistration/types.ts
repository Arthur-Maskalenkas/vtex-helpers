export type MasterDataCLBodyPost = {
  email: string,
  document: string,
  attach?: string,
  saram: string, // campo criado
  approved: boolean, // campo criado
  gender: string, // campo criado
  age: string, // campo criado
  weight: string, // campo criado
  height: string, // campo criado
  chestMeasure: string, // campo criado
  waistMeasure: string, // campo criado
}

